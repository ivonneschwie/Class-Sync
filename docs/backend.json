
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "type": "object",
      "description": "Represents a user's profile.",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "User's email address."
        },
        "displayName": {
            "type": "string",
            "description": "User's public display name."
        },
        "firstName": {
            "type": "string",
            "description": "User's first name."
        },
        "lastName": {
            "type": "string",
            "description": "User's last name."
        },
        "address": {
            "type": "string",
            "description": "User's address."
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp of when the user account was created."
        }
      },
      "required": ["email", "displayName", "createdAt"]
    },
    "Class": {
      "title": "Class",
      "type": "object",
      "description": "Represents a class or course for a specific user.",
      "properties": {
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "code": { "type": "string" },
        "instructor": { "type": "string" },
        "location": { "type": "string" },
        "description": { "type": "string", "description": "An optional description for the class." },
        "accentColor": { "type": "string" },
        "schedule": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "days": { "type": "array", "items": { "type": "string" } },
              "startTime": { "type": "string" },
              "endTime": { "type": "string" }
            },
            "required": ["days", "startTime", "endTime"]
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "name", "code", "instructor", "location", "schedule", "createdAt"]
    },
    "NoteSummary": {
      "title": "Note Summary",
      "type": "object",
      "description": "Represents a summarized version of lecture notes.",
      "properties": {
        "userId": { "type": "string" },
        "subject": { "type": "string" },
        "title": { "type": "string" },
        "notes": { "type": "string" },
        "summary": { "type": "string" },
        "clarificationQuestions": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "subject", "title", "notes", "summary", "createdAt"]
    },
    "FlashcardDeck": {
        "title": "Flashcard Deck",
        "type": "object",
        "description": "A collection of flashcards for a specific subject.",
        "properties": {
            "userId": { "type": "string" },
            "name": { "type": "string" },
            "subject": { "type": "string" },
            "flashcards": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "front": { "type": "string" },
                        "back": { "type": "string" }
                    },
                    "required": ["front", "back"]
                }
            },
            "createdAt": { "type": "string", "format": "date-time" }
        },
        "required": ["userId", "name", "subject", "flashcards", "createdAt"]
    },
    "SharedItem": {
      "title": "Shared Item",
      "type": "object",
      "description": "Stores a publicly shareable version of a resource.",
      "properties": {
        "type": { "type": "string", "enum": ["summary", "deck"] },
        "data": { "type": "object" },
        "originalId": { "type": "string", "description": "The ID of the original document that was shared to prevent duplicates." },
        "createdBy": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["type", "data", "originalId", "createdBy", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/entities/UserProfile" },
          "description": "Stores user profile information. Doc ID is the Firebase Auth UID."
        }
      },
      {
        "path": "/users/{userId}/classes/{classId}",
        "definition": {
          "entityName": "Class",
          "schema": { "$ref": "#/entities/Class" },
          "description": "Stores classes created by a specific user."
        }
      },
      {
        "path": "/users/{userId}/summaries/{summaryId}",
        "definition": {
          "entityName": "NoteSummary",
          "schema": { "$ref": "#/entities/NoteSummary" },
          "description": "Stores note summaries created by a specific user."
        }
      },
      {
        "path": "/users/{userId}/decks/{deckId}",
        "definition": {
          "entityName": "FlashcardDeck",
          "schema": { "$ref": "#/entities/FlashcardDeck" },
          "description": "Stores flashcard decks created by a specific user."
        }
      },
      {
        "path": "/shares/{shareId}",
        "definition": {
          "entityName": "SharedItem",
          "schema": { "$ref": "#/entities/SharedItem" },
          "description": "Stores shared resources accessible via a short code."
        }
      }
    ]
  }
}
